/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/nbaxerrsite',
  images: {
    unoptimized: true,
  },
  assetPrefix: '/nbaxerrsite/',
}

module.exports = nextConfig 